services:
  traefik:
    container_name: traefik
    image: traefik:v3.7.9
    restart: unless-stopped
    env_file: secrets.enc.env
    ports:
      - 80:80
      - 8080:8080
      - 443:443
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ./traefik/traefik.yml:/traefik.yml:ro
      - /home/diego/services_data/networking/traefik/acme.json:/acme.json
      - ./traefik/config.yml:/config.yml:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.entrypoints=http"
      - "traefik.http.routers.traefik.rule=Host(`traefik.local.dynabase.nl`)"
      - "traefik.http.middlewares.traefik-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.sslheader.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.routers.traefik.middlewares=traefik-https-redirect"
      - "traefik.http.routers.traefik-secure.entrypoints=https"
      - "traefik.http.routers.traefik-secure.rule=Host(`traefik.local.dynabase.nl`)"
      - "traefik.http.routers.traefik-secure.tls=true"
      - "traefik.http.routers.traefik-secure.tls.certresolver=cloudflare"
      - "traefik.http.routers.traefik-secure.tls.domains[0].main=local.dynabase.nl"
      - "traefik.http.routers.traefik-secure.tls.domains[0].sans=*.local.dynabase.nl"
      - "traefik.http.routers.traefik-secure.tls.domains[1].main=dynabase.nl"
      - "traefik.http.routers.traefik-secure.tls.domains[1].sans=*.dynabase.nl"
      - "traefik.http.routers.traefik-secure.service=api@internal"
      # Traefik's file provider hot-reloads config.yml (dynamic config) via watch:true,
      # so config.yml edits don't need a container recreate. traefik.yml is STATIC config
      # (applied only at startup), so it's intentionally NOT ignored — edits recreate.
      - "cd.doco.deployment.recreate.ignore={bindMounts: [/config.yml]}"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  traefik-pihole-dns-sync:
    image: ghcr.io/rpressiani/traefik-pihole-dns-sync:v0.1.0
    container_name: traefik-pihole-dns-sync
    restart: unless-stopped
    env_file: secrets.enc.env
    environment:
      - TRAEFIK_API_URL=http://traefik:8080/api/http/routers
      - SYNC_INTERVAL=@every 1m

  # Auto-creates proxied Cloudflare CNAMEs (-> the home_server tunnel) for every public
  # *.dynabase.nl Traefik router (excluding *.local, which is Pi-hole's job), so exposing
  # a service needs no manual DNS. Traefik-poll only: ENABLE_DOCKER_POLL is left EMPTY on
  # purpose -- the tool treats ANY non-empty string (even "FALSE") as truthy and would then
  # open the Docker socket, which we don't mount. No healthcheck, matching the
  # traefik-pihole-dns-sync sidecar above (s6 supervises; restart covers the container).
  # Reuses the cert-resolver's token: add CF_TOKEN to secrets.enc.env with the SAME value
  # as CF_DNS_API_TOKEN (both need Zone:DNS:Edit on dynabase.nl).
  traefik_cloudflare_companion:
    container_name: traefik_cloudflare_companion
    image: docker.io/tiredofit/traefik-cloudflare-companion:7.4.0
    restart: unless-stopped
    env_file: secrets.enc.env
    environment:
      - ENABLE_DOCKER_POLL=
      - ENABLE_TRAEFIK_POLL=TRUE
      - TRAEFIK_VERSION=2
      - TRAEFIK_POLL_URL=http://traefik:8080
      - TRAEFIK_POLL_SECONDS=60
      - TARGET_DOMAIN=8b7293d9-91d4-4245-83ac-2bcf2aa57f1c.cfargotunnel.com
      - DOMAIN1=dynabase.nl
      - DOMAIN1_ZONE_ID=fd6e52b06c2f0fbe6c343ba00ad99c12
      - DOMAIN1_PROXIED=TRUE
      - RC_TYPE=CNAME
      - REFRESH_ENTRIES=TRUE
      - TRAEFIK_INCLUDED_HOST1=.*\.dynabase\.nl
      - TRAEFIK_EXCLUDED_HOST1=.*\.local\.dynabase\.nl

  cloudflare_tunnel:
    container_name: cloudflare_tunnel
    image: cloudflare/cloudflared:2026.7.3
    restart: unless-stopped
    env_file: secrets.enc.env
    # cloudflared's TUNNEL_TOKEN env var is silently rejected by v2026.3.0;
    # pass the token explicitly on the command line instead.
    command: ["tunnel", "--no-autoupdate", "run", "--token-file", "/run/secrets/tunnel_token"]
    environment:
      TUNNEL_METRICS: "0.0.0.0:60123"
    secrets:
      - tunnel_token
    healthcheck:
      test: ["CMD", "cloudflared", "tunnel", "ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

secrets:
  tunnel_token:
    file: /home/diego/.config/cloudflared/tunnel_token

networks:
  default:
    external: true
    name: home_server_network
